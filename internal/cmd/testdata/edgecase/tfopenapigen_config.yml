# Ref: https://registry.terraform.io/providers/integrations/github/latest/docs
provider:
  name: edgecase

resources:

data_sources:
  nested_lists:
    read:
      path: /nested_lists
      method: GET